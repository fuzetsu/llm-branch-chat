# Verify Build

Runs complete validation pipeline: linting, type checking, and build verification.

```bash
npm run verify-build
```

This command ensures code quality, type safety, and build success in one step.
